spring:
  datasource:
    url: jdbc:mysql://mysql:3306/ultralearning?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: ultralearning
    password: ultralearning123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  jackson:
    default-property-inclusion: non_null

server:
  port: 8080

ai-service:
  base-url: http://ai-service:8000

jwt:
  secret: ultralearning-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm
  expiration: 86400000  # 24 hours

logging:
  level:
    com.ultralearning: DEBUG
